[project]
name = "fast-dcp"
version = "0.0.1"
authors = [{name="Hiro Ito", email="mobiiii24@gmail.com"}]
description = "Aliases docker compose commands and arguments."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.scripts]
dcp = "fast_dcp:main"
dcpu = "fast_dcp:dcpu_main"
dcpe = "fast_dcp:dcpe_main"

[project.urls]
Repository = "https://github.com/HiroItozzz/fast-dcp"

[build-system]
requires = ["flit_core >= 3.12.0, <4"]
build-backend = "flit_core.buildapi"

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "ruff>=0.15.12",
    "ty>=0.0.34",
]

[tool.uv]
package = true

[tool.ruff]
line-length=80

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ty.src]
include= ["src/"]